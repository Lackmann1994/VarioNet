'use strict';

// Register `optionpanel` component, along with its associated controller and template
angular.
module('optionpanel').
component('optionpanel', {
    templateUrl: 'optionpanel/optionpanel.template.html',
    transclude: true,
    controller: [
        function optionpanelController() {

        }]
});
